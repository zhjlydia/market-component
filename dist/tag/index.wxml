<wxs src="../wxs/utils.wxs" module="utils" />

<view
  class="custom-class {{ utils.bem('tag', [size, { mark, plain, round }]) }} {{ plain ? 'spui-border--surround' : '' }}"
  style="{{ style }}"
>
  <slot />
</view>
