<view
  wx:if="{{ animated || inited }}"
  class="custom-class spui-tab__pane {{ active ? 'spui-tab__pane--active' : 'spui-tab__pane--inactive' }}"
  style="{{ animated || active ? '' : 'display: none;' }} {{ width ? 'width:' + width + 'px;' : '' }}"
>
  <slot />
</view>
